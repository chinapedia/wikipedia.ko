> This article is converted from Wikipedia: [틀:항공기 제원](https://ko.wikipedia.org/wiki/틀:항공기_제원).


<onlyinclude><includeonly>{{\#if:|

정보의 출처:

|}}<big style="padding-top: .2em;">일반 특성</big> {{

1.  if:

`|* `**`승무원:`**` `
`}}{{`

1.  if:

`|* `**`승무원:`**` `
`}}{{`

1.  if:

`|* `**`용량:`**` `
`}}{{`

1.  if:

`|* `**`화물탑재량:`**` `` (``) `
`}}{{`

1.  if:

`|* `**`길이:`**` `` (``) `
`}}{{`

1.  if:

`|* `**`길이:`**` `
`}}{{`

1.  ifeq: | copter

`|* `**`로터직경:`**` `` (``) `
`|* `**`날개폭:`**` `` (``)`
`}}{{`

1.  if:

`|* `**`높이:`**` `` (``) `
`}}{{`

1.  if:

`|* `**`높이:`**` `
`}}{{`

1.  if:

`|{{`
`#ifeq:`` | copter`
` |* `**`원판면적:`**` `` (``)`
` |* `**`날개면적:`**` `` (``)`
` }}`
`}}{{`

1.  if:

`|* `**`날개꼴:`**` `
`}}{{`

1.  if:

`|* `**`날개꼴:`**` ``     `
`}}{{`

1.  if:

`|* `**`공허중량:`**` `` (``)`
`}}{{`

1.  if:

`|* `**`공허중량:`**` `
`}}{{`

1.  if:

`|* `**`탑재중량:`**` `` (``)`
`}}{{`

1.  if:

`|* `**`유효탑재량:`**` `` (``)`
`}}{{`

1.  if:

`|* `**`최대이륙중량:`**` `` (``) `
`}}{{`

1.  if:

`|* `**`최대이륙중량:`**` `` `
`}}{{`

1.  if:

`|* `**`레이더:`**` `
`}}{{`

1.  switch:

`|prop =* `**`엔진:`**` ``× ``{{#if: `` | `` }}, `` (``)`` `` {{`
`#ifeq: `` |1 |`
` |각각`
`}}{{`
`#if: `
` |* `**`프로펠러:`**` `` ``{{`
` #if: `
`  |, `` 엔진 마다`
` }}`
`}}{{`
`#if:`
` |{{ #if: `` |**|*}} `**`프로펠러``   ``직경:`**` `` (``)`
`}}`
`| jet =* `**`엔진:`**` ``× `` ``{{`
` #if: `
`  |{{`
`   #if: `
`    |** `**`최대추력:`**` `` (``)`` {{`
`    #ifeq: `` |1`
`     |`
`     |각각`
`    }}`

  -   - **재연소시추력:**  ()  \<\!--

`   follows ELSE branch of if [NOT] afterburning`

` -->`
`    |, `` (``)`` `
`   }}  {{`
`  #ifeq: `` | 1 |`
`   |각각 }}`
`}}`
`|both=* `**`엔진:`**

  -   - × {{\#if:  |  }},  ()  {{

`#ifeq:``|1`
` |`
` |각각`
`}}`

  -   - × {{\#if:  |  }},  ()  {{

`#ifeq:``|1`
` |`
` |각각`
`}}{{`
`#if: `
` |*** `**`프로펠러:`**` `` ``{{`
` #if:`
`  |, `` 엔진 마다`
` }}{{`
` #if: `
`  |** `**`프로펠러``   ``직경:`**` `` (``)`
` }}`
`}}{{`
`#if:`
` |{{`
` #if: `
`  |`
`  |* `**`프로펠러``   ``직경:`**` `` (``)`
` }}`
`}}`

}}{{

`#if: `
` |* `

}} <big></big> {{

1.  if:

`|* `**`초과금지속도:`**` `` {{`

1.  if:

`|(``) `
`}}`
`}}{{`

1.  if:

`|* `**`초과금지속도:`**` `` `
`}}{{`

1.  if:

`|* `**`최대속도:`**` `` {{ `
`#if:`
`| (``) `
`}}`
`}}{{`

1.  if:

`|* `**`최대속도:`**` `` `
`}}{{`

1.  if:

`|* `**`순항속도:`**` `` {{`
`#if:`
`| (``) `
`}}`
`}}{{`

1.  if:

`|* `**`실속속도:`**` `` (``) `
`}}{{`

1.  if:

`|* `**`실속속도:`**` `` `
`}}{{`

1.  if:

`|* `**`항속거리:`**` `` (``) `
`}}{{`

1.  if:

`|* `**`항속거리:`**` `
`}}{{`

1.  if:

`|* `**`전투행동반경:`**` `` (``) `
`}}{{`

1.  if:

`|* `**`전투행동반경:`**` `` `
`}}{{`

1.  if:

`|*`**`전투항속거리:`**` `` (``) `
`}}{{`

1.  if:

`|* `**`최대항속거리:`**` `` (``) `
`}}{{`

1.  if:

`|* `**`상승한도:`**` `` (``) `
`}}{{`

1.  if:

`|* `**`상승률:`**` `` (``) `
`}}{{`

1.  if:

`|* `**`하강률:`**` `` (``) `
`}}{{`

1.  if:

`|* `**`활공률:`**` `
`}}{{`

1.  if:

`|* '''{{#ifeq: `` |copter`
`        |원판하중`
`         |날개하중`
`        }}:''' `` (``)`
`}}{{`

1.  if:

`|* `**`양항비:`**` `
`}}{{`

1.  if:

`|* `**`연료소비:`**` `
`}}{{`

1.  switch:

`|jet={{ #if: `
` |* `**`추력대중량비:`**`{{`
` #if:`
`  |** `**`Dry:`**` `
` }}`

  -   - **재연소시:**

` |{{`
` #if:`
`  |* `**`추력대중량비:`**` `
` }}`
`}}`
`|prop={{`
`#if:`
` |* `**`출력대중량비:`**` `` (``)`
`}}`
`|both={{`
`#if:`
` |* `**`출력대중량비``   ``(프로펠러엔진):`**` `` (``)`
`}}{{`
`#if:`
` |* `**`출력대중량비``   ``(제트엔진):`**` `
`}}`
`}}{{`

1.  if:

`|* '''최대 {{#ifeq: `` |copter`
`           | 원판하중`
`             | 날개하중`
`            }}:''' `` (``)`
`}}{{`

1.  switch:

`|jet={{#if:`
` |* `**`최소``   `[`추력대중량비`](https://ko.wikipedia.org/wiki/추력대중량비 "wikilink")`:`**`{{`
` #if:`
`  |** `**`Dry:`**` `
` }}`

  -   - **재연소시:**

` |{{`
` #if:`
`  |* `**`최소``   `[`추력대중량비`](https://ko.wikipedia.org/wiki/추력대중량비 "wikilink")`:`**` `
` }}`
`}}`
`|prop={{`
`#if:`
` |* `**`최소``   `[`출력대중량비`](https://ko.wikipedia.org/wiki/출력대중량비 "wikilink")`:`**` `` (``)`
`}}`
`|both={{`
`#if:`
` |* `**`최소``   `[`출력대중량비`](https://ko.wikipedia.org/wiki/출력대중량비 "wikilink")`   ``(프로펠러엔진):`**` `` (``)`
`}}{{`
`#if:`
` |* `**`최소``   `[`추력대중량비`](https://ko.wikipedia.org/wiki/추력대중량비 "wikilink")`   ``(제트엔진):`**` `
`}}`
`|`

}}{{

1.  if:

`|`<big></big>

`  |{{#if:`
`   |* `**`기관포:`**` `
`   }}{{#if:`
`   |* `**`로켓:`**` `
`   }}{{#if:`
`   |* `**`미사일:`**` `
`   }}{{#if:`
`   |* `**`폭탄:`**` `
`   }}`
`  }}`
` }}}`
`}}`

{{\#if:

`|`<big>[`항공전자장비`](https://ko.wikipedia.org/wiki/항공전자장비 "wikilink")</big>

 }}{{

1.  if: |

 }}{{

1.  if:

`|{{`
`#if: `
` |`

</ul>

`}}`<big></big>

`  |* `**`기관포:`**` `
` }}{{`
` #if:`
`  |* `**`무기장착점:`**` `` {{#if:`
`   | ``,`
`  }}{{`
`  #if:`
`   |다음의 조합이 가능함:`
`  }}{{`
`  #if:`
`   |** `**`로켓:`**` `
`  }}{{`
`  #if:`
`   |** `**`미사일:`**` `
`  }}{{`
`  #if:`
`   |** `**`폭탄:`**` `
`  }}{{`
`  #if:`
`   |** `**`기타:`**` `
`  }}`
` }}{{`
` #if:`
`  |* `**`로켓:`**` `
` }}{{`
` #if:`
`  |* `**`미사일:`**` `
` }}{{`
` #if:`
`  |* `**`폭탄:`**` `
` }}`
` `

</ul>

}}} }}{{

1.  if:

`|{{`
`#if: `
` |`

</ul>

`}}`

</ul>

<big></big>

`}}`</includeonly></onlyinclude>
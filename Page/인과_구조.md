> This article is converted from Wikipedia: [인과 구조](https://ko.wikipedia.org/wiki/인과_구조).


[일반 상대성 이론에서](https://ko.wikipedia.org/wiki/일반_상대성_이론 "wikilink"), **인과 구조**(因果構造, )는 [시공간](https://ko.wikipedia.org/wiki/시공간 "wikilink")의 점들 사이에, [상대성 이론에](https://ko.wikipedia.org/wiki/상대성_이론 "wikilink") 따라 어떤 점이 다른 어떤 점에 물리적으로 영향을 줄 수 있는지에 대한 관계를 나타낸다.

## 정의

[로런츠 다양체](https://ko.wikipedia.org/wiki/로런츠_다양체 "wikilink") \((M,g)\)가 주어졌다고 하고, [계량 부호수를](https://ko.wikipedia.org/wiki/계량_부호수 "wikilink") −+++로 잡자.

### 벡터의 분류

어떤 점 \(x\in M\)에서의 벡터 \(v\in T_xM\)는 다음과 같이 분류된다.

  - 만약 \(g(v,v)<0\)이라면 \(v\)를 **시간꼴**()이라고 한다.
  - 만약 \(g(v,v)=0\)이라면 \(v\)를 **영벡터**()라고 한다.
  - 만약 \(g(v,v)>0\)이라면 \(v\)를 **공간꼴**()이라고 한다.

시간꼴/영/공간꼴 벡터장은 모든 점에서 시간꼴/영벡터/공간꼴인 [벡터장](../Page/벡터장.md "wikilink")이다. **인과 벡터장**()은 모든 점에서 시간꼴이거나 영벡터인 벡터장이다.

만약 어떤 곡선 \(\gamma(t)\)의 접벡터 \(d\gamma/dt\)가 항상 시간꼴이라면, 이를 **시간꼴 곡선**이라고 한다. 마찬가지로 **영벡터 곡선**이나 **공간꼴 곡선**도 정의할 수 있다. **인과적 곡선**()은 모든 점에서 접벡터가 영벡터이거나 시간꼴인 곡선이다.

### 시간 방향

점 \(x\in M\)에서, 시간꼴 벡터 \(v\in T_xM\)들의 집합에 다음과 같은 [동치 관계를](https://ko.wikipedia.org/wiki/동치_관계 "wikilink") 주자.

\[u\sim v\Leftrightarrow g(u,v)<0\] 이에 따라, \(x\)에서의 모든 시간꼴 벡터들은 두 [동치류](https://ko.wikipedia.org/wiki/동치류 "wikilink")로 분류할 수 있다. \(x\)에서의 **시간 방향**은 이 둘 가운데 하나를 선택한 것이며, 선택된 방향을 **미래 방향**, 선택되지 않은 방향을 **과거 방향**이라고 한다.

만약 로런츠 다양체 \(M\) 전체에 시간 방향을 연속적으로 줄 수 있다면 \(M\)을 **시간 가향**()하다고 한다.

**미래 방향 시간꼴 벡터장**()은 모든 점에서 미래 방향 벡터 값을 갖는 시간꼴 벡터장이다. 마찬가지로, **과거 방향 시간꼴 벡터장**()은 모든 점에서 과거 방향 벡터 값을 갖는 시간꼴 벡터장이다.

### 인과 관계

두 점 \(x,y\in M\) 사이에 다음과 같은 관계를 정의할 수 있다.

  - 만약 \(x\)에서 \(y\)로 가는 미래 방향 시간꼴 곡선이 존재한다면 \(x\)가 \(y\)보다 **시간 순으로 선행한다**()고 하고, \(x\ll y\)라고 쓴다.
  - 만약 \(x\)에서 \(y\)로 가는 미래 방향 인과 곡선이 존재한다면 \(x\)가 \(y\)보다 **인과적으로 선행한다**()고 하고, \(x\prec y\)라고 쓴다.

마찬가지로, 그 반대 개념인 **시간 순 후행** (\(x\gg y\)) 및 **인과적 후행**(\(x\succ y\))도 정의할 수 있다.

이들은 [추이법칙을](https://ko.wikipedia.org/wiki/추이관계 "wikilink") 만족시킨다. 즉

\[x \ll y,y\ll z\implies x\ll z\]

\[x\prec y,y\prec z\implies x\prec z\] 또한, 인과적 선행은 시간 순 선행보다 더 약한 개념이다.

\[x\ll y\implies x\prec y\]

\[x\ll y,y\prec z\implies x\ll z\]

\[x\prec y,y\ll z\implies x\ll z\]

시간순/인과적 선행을 사용하여, 다음과 같은 미래 및 과거 개념을 정의할 수 있다.

  - 점 \(x\)의 **시간 순 미래** \(I^+(x)\)는 \(x\)보다 시간 순으로 후행하는 점들의 집합이다.

\[I^+(x)=\{y\in M\colon x\ll y\}\]

  - 점 \(x\)의 **시간 순 과거** \(I^-(x)\)는 \(x\)보다 시간 순으로 선행하는 점들의 집합이다.

\[I^-(x)=\{y\in M\colon x\gg y\}\]

  - 점 \(x\)의 **인과적 미래** \(J^+(x)\)는 \(x\)보다 인과적으로 후행하는 점들의 집합이다.

\[J^+(x)=\{y\in M\colon x\prec y\}\]

  - 점 \(x\)의 **인과적 과거** \(J^-(x)\)는 \(x\)보다 인과적으로 선행하는 점들의 집합이다.

\[J^-(x)=\{y\in M\colon x\succ y\}\]

### 점근적 과거와 미래

시공간 \((M,g)\)를 등각 콤팩트화 \((M,g)\hookrightarrow(\tilde M,g)\)할 수 있다고 하자. 즉,

\[\tilde M=M\sqcup\partial\tilde M\] 이다. 이 경우

  - \(\tilde M\)의 **미래 시간꼴 무한**() \(i^+\subset\partial\tilde M\)는 \(M\)에서 시작하는 시간꼴 곡선들의 미래 방향 끝점들의 집합이다.
  - \(\tilde M\)의 **과거 시간꼴 무한**() \(i^-\subset\partial\tilde M\)는 \(M\)에서 끝나는 시간꼴 곡선들의 과거 방향 끝점들의 집합이다.
  - \(\tilde M\)의 **공간꼴 무한**() \(i^0\subset\partial\tilde M\)은 \(M\)에서 시작하는 공간꼴 곡선들의 끝점들의 집합이다.
  - \(\tilde M\)의 **미래 영벡터 무한**() \(\mathcal I^+\subset\partial\tilde M\)은 \(M\)에서 시작하는 영벡터 곡선들의 미래 방향 끝점의 집합이다. 이는 보통 "스크라이 플러스"로 발음하는데, 이는 [LaTeX](../Page/LaTeX.md "wikilink") 매크로 `{\scr I}^+`에서 유래한다.
  - \(\tilde M\)의 **과거 영벡터 무한**() \(\mathcal I^-\subset\partial\tilde M\)은 \(M\)에서 끝나는 영벡터 곡선들의 과거 방향 끝점의 집합이다. 이는 보통 "스크라이 마이너스"로 발음하는데, 이는 [LaTeX](../Page/LaTeX.md "wikilink") 매크로 `{\scr I}^-`에서 유래한다.

## 참고 문헌

  -
  -
  -
[분류:일반 상대성이론](https://ko.wikipedia.org/wiki/분류:일반_상대성이론 "wikilink") [분류:미분기하학](https://ko.wikipedia.org/wiki/분류:미분기하학 "wikilink")
> This article is converted from Wikipedia: [틀:Module rating](https://ko.wikipedia.org/wiki/틀:Module_rating).


<includeonly>

`     }}`
`  | alpha | a                 = 이 모듈은 `[`알파`](https://ko.wikipedia.org/wiki/:분류:알파_모듈 "wikilink")` 등급이 매겨졌습니다. 이것은 제삼자가 입력할 준비가 되었고, 문제가 발생하는지를 보기 위해서 몇 페이지에 사용될 수 있지만, 지켜보고 있어야 합니다. 새로운 기능이나 입력과 출력 메커니즘에 대한 제안은 환영합니다. {{#switch: ``|doc|sandbox=`
`      | {{#ifeq: `` | true |   | `[](https://ko.wikipedia.org/wiki/분류:알파_모듈 "wikilink")` }}`
`     }}`
`  | beta | b                  = 이 모듈은 `[`베타`](https://ko.wikipedia.org/wiki/:분류:베타_모듈 "wikilink")` 등급이 매겨졌고, 넓게 쓰일 준비가 되었습니다. 이것은 여전히 새롭고 예상된 결과가 보장되는지 주의를 기울여야 할 필요가 있습니다. {{#switch: ``|doc|sandbox=`
`      | {{#ifeq: `` | true |   | `[](https://ko.wikipedia.org/wiki/분류:베타_모듈 "wikilink")` }}`
`     }}`
`  | release | r | general | g = 이 모듈은 `[`일반적으로``   ``사용될`](https://ko.wikipedia.org/wiki/:분류:일반적으로_사용되는_모듈 "wikilink")` 수 있는 등급이 매겨졌습니다. 이것은 발전된 형태에 도달하였으며 오류가`
`없고 어떤 상황에서도 적절하게 사용할 수 있다고 생각할 수 있습니다. 이것은 도움말 페이지에 멘션을 남길 준비가 되었고 새로운 사용자가 배우기 위한 다른 위키백과 자원입니다. 서버 로드와 잘못된 출력을 줄이기 위해서는, 반복적으로 시도와 오류발생하는 편집을 하기보다는 `[`시험장``   ``테스트를`](https://ko.wikipedia.org/wiki/위키백과:틀_연습장과_시험장 "wikilink")` 통해서 발전시켜야 합니다. {{#switch: ``|doc|sandbox=`
`      | {{#ifeq: `` | true |   | `[](https://ko.wikipedia.org/wiki/분류:일반적으로_사용되는_모듈 "wikilink")` }}`
`     }}`
`  | protected | protect | p   = 이 모듈은 `[`보호대상`](https://ko.wikipedia.org/wiki/:분류:보호대상인_모듈 "wikilink")` 등급이 매겨졌습니다. 이것은 매우 많은 문서에 사용되는 `[`가시성이``   ``높은``   ``모듈이거나`](https://ko.wikipedia.org/wiki/위키백과:위험한_틀 "wikilink")` 매우 자주 `[`문서``   ``풀어넣기됩니다`](https://ko.wikipedia.org/wiki/백:풀기 "wikilink")`. 반달이나 실수가 많은 페이지에 영향을 미칠 수 있고 사소한 편집이 서버의 대량 로드가 일어날 수 있기 때문에, 이것은 편집 `[`보호되었습니다`](https://ko.wikipedia.org/wiki/백:보호 "wikilink")`. {{#switch: ``|doc|sandbox=`
`      | {{#ifeq: `` | true |   | `[](https://ko.wikipedia.org/wiki/분류:보호대상인_모듈 "wikilink")` }}`
`     }}`
`  | semiprotected | semiprotect | semi   = 이 모듈은 `[`보호대상`](https://ko.wikipedia.org/wiki/:분류:보호대상인_모듈 "wikilink")` 등급니 매겨졌습니다. 이것은 매우 많은 문서에 사용되는 `[`가시성이``   ``높은``   ``모듈이거나`](https://ko.wikipedia.org/wiki/위키백과:위험한_틀 "wikilink")` 매우 자주 `[`문서``   ``풀어넣기됩니다`](https://ko.wikipedia.org/wiki/백:풀기 "wikilink")`. 반달이나 실수가 많은 페이지에 영향을 미칠 수 있고 사소한 편집이 서버의 대량 로드가 일어날 수 있기 때문에, 이것은 편집 `[`준보호되었습니다`](https://ko.wikipedia.org/wiki/백:준보호 "wikilink")`. {{#switch: ``|doc|sandbox=`
`      | {{#ifeq: `` | true |   | `[](https://ko.wikipedia.org/wiki/분류:보호대상인_모듈 "wikilink")` }}`
`     }}`
`  | #default                  = `
` }}`

}}| [분류:잘못된 이름공간에 있는 틀이 사용됨](https://ko.wikipedia.org/wiki/분류:잘못된_이름공간에_있는_틀이_사용됨 "wikilink")|데모공간=}}</includeonly><noinclude>   </noinclude>
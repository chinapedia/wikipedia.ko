> This article is converted from Wikipedia: [:  ](https://ko.wikipedia.org/wiki/:__).


  - 이 틀에는 이 사용되고 있습니다. \<\!--

`      --------- Different texts depending on template's default state ---------`
` -->규정치에서는 모든 목록이  {{#ifeq:``|all`
`     |전개되고 있습니다.`
`      |접어지고 있습니다.  `

<div style="padding-bottom:0.75em;">

모든 목록을 전개시키고 싶은 경우는 `{{((}}`<noinclude>`틀 이름`</noinclude><includeonly></includeonly>`  |expanded=all}} `이라고 입력합니다.
<span style="font-size:100%;">(혹은 `{{((}}`<noinclude>`틀 이름`</noinclude><includeonly></includeonly>`  |all}} `과 "expanded="를 뽑는 것도 가능합니다)</span>.

</div>

}}

<div style="padding-bottom:0.75em;">

특정의 목록만을 전개시켜 다른 목록은 정리한 채로 하고 싶은 경우는
`{{((}}`<noinclude>`틀 이름`</noinclude><includeonly></includeonly>`  {{!}}expanded= `*`목록명`*`}}`이라고 입력합니다.<span style="font-size:100%;">(혹은 `{{((}}`<noinclude>`틀 이름`</noinclude><includeonly></includeonly>`  {{!}} `*`목록명`*`}}`도 가능합니다)</span>
*목록명*은 다음과 같습니다:

<div style="padding:0.25em 0;">

</div>

(쉼표, 인용부호는 붙여서는 안됩니다).

</div>

{{\#if:

`          | `<noinclude>`[`</noinclude>` 예로서 {{((}}`<noinclude>`틀 이름`</noinclude><includeonly></includeonly>`  {{!}}expanded= ``{{))}} `
<noinclude></noinclude><span style="font-size:100%;">`(혹은 {{((}}`<noinclude>`틀 이름`</noinclude><includeonly></includeonly>`  {{!}} ``{{))}} )`</span>`. }} `<noinclude>`]`

</noinclude>